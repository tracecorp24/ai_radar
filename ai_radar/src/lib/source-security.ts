export function isSafeSourceUrl(type: string, value: string) { try { const url = new URL(value); if (!['http:','https:'].includes(url.protocol)) return false; const host = url.hostname.toLowerCase(); const loopback = host === 'localhost' || host === '127.0.0.1' || host === '::1'; if (loopback && !type.toLowerCase().includes('local')) return false; return true; } catch { return false; } }
