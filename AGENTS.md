# Agent entry point

Use `CLAUDE.md` in this directory as the compact workspace map. Read the nearest scoped `CLAUDE.md` or existing `AGENTS.md` before changing a subproject; avoid scanning unrelated repositories.

